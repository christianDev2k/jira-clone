import styled from 'styled-components';

const CreateProject = () => {
    return (
        <div className='p-4'>
            <ContentContainer className='bg-[var(--white-color)] p-4 rounded-lg'>
                <h1 className='text-center font-bold text-3xl'>Create Project</h1>

                <form>
                    
                </form>
            </ContentContainer>
        </div>
    );
};

const ContentContainer = styled.div``;

export default CreateProject;
